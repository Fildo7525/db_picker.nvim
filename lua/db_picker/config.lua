return {
	directories = {
		-- Place for defining directories on special places.
	},
	default = {
		search_directory = ".",
		search_pattern = "build",
	},
	lsp_client_path = "usr.lsp.settings.clangd",
}
